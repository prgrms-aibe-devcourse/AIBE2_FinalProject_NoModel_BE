<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <timestamp key="BY_DATE" datePattern="yyyy-MM-dd"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] %green([%thread]) %highlight(%-5level) %boldWhite([%C.%M:%yellow(%L)]) - %msg%n"/>
    <property name="FILE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] [%thread] %-5level [%C.%M:%L] - %msg%n"/>

    <springProfile name="local">
        <include resource="log/console-appender.xml"/>
        <include resource="log/logstash-local-appender.xml"/>

        <logger name="nomodel" level="DEBUG" />
        <logger name="org.springframework.web" level="INFO" />
        <logger name="org.springframework.security" level="INFO" />
        <logger name="org.springframework" level="INFO" />
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="LOGSTASH-LOCAL"/>
        </root>
    </springProfile>

    <springProfile name="dev">
        <include resource="log/console-appender.xml"/>
        <include resource="log/file-info-appender.xml"/>
        <include resource="log/file-warn-appender.xml"/>
        <include resource="log/file-error-appender.xml"/>
        <include resource="log/logstash-dev-appender.xml"/>

        <logger name="nomodel" level="DEBUG" />
        <logger name="org.springframework" level="INFO" />
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="LOGSTASH-DEV"/>
            <appender-ref ref="FILE-INFO"/>
            <appender-ref ref="FILE-WARN"/>
            <appender-ref ref="FILE-ERROR"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <!-- Use async appender for maximum throughput in production -->
        <include resource="log/logstash-prod-async-appender.xml"/>

        <logger name="nomodel" level="INFO" />
        <logger name="org.springframework" level="WARN" />
        <logger name="org.hibernate" level="WARN" />
        <logger name="com.zaxxer.hikari" level="WARN" />
        <logger name="net.logstash.logback" level="ERROR" />
        <root level="INFO">
            <appender-ref ref="LOGSTASH-PROD"/>
        </root>
    </springProfile>
</configuration>
